version: 2

models:
    - name: dim_customers
      columns:
        - name: customer_id
          tests:
            - not_null
            - unique
        - name: country
          tests:
            - not_null

    - name: dim_products
      columns:
        - name: stock_code
          tests:
            - not_null
            - unique
            